# 现在需要人类验证的部分

Windows 代码、构建和便携包接口检查已完成。接下来用一台电脑、一部手机、一个不重要的项目操作即可；失败时记下步骤和实际表现，不需要逐屏截图。

## 先验证这台电脑

1. 双击 `start.cmd` 打开电脑状态和二维码。手机连接同一 Wi-Fi 后扫码。确认 Remote 首页能看到正确电脑、项目和最近会话。
2. 手机选择项目开始新聊天，发送“只读查看这个项目，概括用途和目录结构，暂时不要改文件”。确认回复、模型和项目正确。
3. 在官方桌面另开一个测试会话并启动任务，运行期间在手机打开同一会话。确认打开时电脑继续输出；发送补充，确认原任务收到；再用一个可停止的测试任务验证停止按钮。
4. 在测试会话触发一次原生审批或用户问题，用手机回答。再验证电脑先处理后，手机旧卡片不再可用。完全访问模式没有触发审批时，这项保持未测。
5. 锁屏后返回，再切换 Wi-Fi / 蜂窝网络，确认任务继续、内容恢复、文字草稿保留、已发消息不重复。蜂窝网络必须使用你部署的公网中继，局域网地址无法覆盖这项。
6. 检查键盘弹起、输入框、菜单、返回、历史翻页和文件预览。网页麦克风只引导使用系统键盘听写。

## 再验证安装和自己的底模

- 新目录解压便携包，双击 `start.cmd`，通过窗口选择项目和中继；核对保存、关闭后重开、状态页、停止与可选登录自启。无需安装本项目的开发依赖。配置窗口的实际布局和操作未做自动 UI 验收。
- 用已经能在电脑 Codex 中运行的 DeepSeek 配置完成新聊天及继续聊天。确认未要求额外 OpenAI 登录，且实际模型、项目和凭据来源正确。当前环境没有真实 DeepSeek 配置；本地模拟服务通过不等于 DeepSeek 实测通过。
- 自托管中继需你自己的服务器和可访问域名，按 QUICKSTART 部署后验证公网连接、换网和 Token 轮换。当前机器的 Docker 引擎未运行，容器与 HTTPS 部署尚未实测。

## 最小反馈格式

```text
电脑系统 / 手机系统及浏览器：
Codex CLI / 官方桌面 / 包内 VERSION：
模型服务及模型名（不要贴 Key 或 Token）：
执行了哪一步：
结果或失败表现：
```

真实桌面写操作、审批、手机锁屏/换网和第三方服务通过后，才能把对应组合标为已验收。完整场景表保留在 IMPLEMENTATION.md。
