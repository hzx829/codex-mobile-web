# Codex Mobile Web 实施计划

状态：P0–P4 核心代码已实现、接口验证通过；P5 已有 Windows 便携构建；应用验收待人工完成。更新日期：2026-09-06。

目标与范围以 [项目说明](README.md) 为准，实现约束见 [设计方案](DESIGN.md)。本计划用于一个人配合 LLM 顺序推进；不设团队分工、冲刺仪式或预估工期。

## 执行方式

- 每次处理一个能交付并验证的任务，优先复用已选底座。实现中若需要改设计，同步修改对应文档。
- LLM 负责代码、类型检查、必要的协议与故障测试，以及简短人工验收步骤。人类负责应用实际操作和截图；不安排自动浏览器截图巡检。
- 接口模拟测试证明本项目逻辑，真实协议验证证明与指定 Codex 版本兼容，人工操作证明手机体验。三者不能互相替代。
- 实施时在本文件更新阶段状态与最少证据：代码提交、实际执行检查、结果、尚未通过的人工用例。现有检查与边界见底部实施记录，代码变更通过本项目 Git 仓库记录。
- 状态使用“未开始 / 进行中 / 待人工验收 / 已验收 / 阻塞”。没有人工结果时保持待验收；依赖该结果的决策暂不作成功假设。

## 阶段顺序

| 阶段 | 交付结果 | 依赖 | 当前状态 |
| --- | --- | --- | --- |
| P0 | Windows 可行性证据、支持范围和唯一实施底座 | 无 | 待人工验收 |
| P1 | 本机配置继承与会话控制可用 | P0 路径可行 | 待人工验收 |
| P2 | 手机 Web 核心体验可操作 | P1 | 待人工验收 |
| P3 | 发送与断线恢复可靠 | P2 | 待人工验收 |
| P4 | 用户可以简单部署并连接自己的中继 | P3 | 待人工验收 |
| P5 | Windows 安装分发与版本管理可用 | P4 | 进行中（便携构建已完成） |
| P6 | 固定组合的小范围真实试用 | P5 | 未开始 |

macOS 可在 P0 的平台调整条件触发时提前验证，否则在 Windows 完成 P5 后补同等安装与验收。基础模块可按依赖继续实现，完整发布必须通过相关人工验收。

## P0 验证路径并确定底座

先证明关键能力，验证范围为一台电脑、一个测试项目、一个空闲会话和一个活动会话。不改现有 Happier 配置，不在重要活动会话上试验停止或审批。

### P0.1 记录真实环境

- [x] 记录 Windows 版本、Codex CLI 路径与版本、官方桌面版本、运行用户及配置目录。
- [x] 区分 Windows 原生与 WSL 的 Codex 安装、配置和项目路径，首验只选一种执行环境。
- [x] 检查当前第三方模型是否已在目标 Codex 中可用；记录 provider、模型、配置方案和认证来源的类型，不输出凭据。
- [x] 准备一个非重要测试项目。已有 DeepSeek 配置可直接复用；没有时先完成无凭据的协议检查，真实模型验收等待用户提供可用配置，不冒称通过。

产出：在本文件“实施记录”填写脱敏环境基线。配置继承的成功判据是运行时实际使用目标模型，单独读到配置文本不能算通过。

### P0.2 检查复用候选

- [x] 将 Codex Anywhere 固定到一个提交，在隔离目录检查模型配置读取、桌面输入、审批、重连和最小连接配置。
- [x] 核对许可证及复用要求，保留来源；目前只查过项目声明，尚未完成固定提交的许可证文件复核。
- [x] 核对 Remodex 的当前桌面控制实现，验证需要的接口及 Windows 支持情况；仅提取必要思路或兼容模块。
- [x] 对照固定版本生成或读取 app-server 协议类型，记录实际存在的输入、中断、审批与恢复接口。

不继续扩展候选列表。若 Codex Anywhere 可以通过局部修改满足核心要求，则选为唯一底座；若必须重写控制主链路，则用相同预选技术实现小型项目，并在此记录弃用的具体原因。复杂权限功能不作为选型加分项，不同时维护两个成品方向。

### P0.3 验证同一会话控制

为下面两类会话分别记录结果：官方桌面发起的活动会话、连接器管理的 app-server 会话。额外记录独立 CLI 活动会话是否存在受支持的接入路径，不能把其历史可读标成活动任务可控。

- [ ] 读取当前任务并接收增量进度，打开观察端不造成中断。
- [ ] 提交一条补充消息，确认进入原任务；若运行端排队，记录原生队列行为。
- [ ] 停止目标任务，确认原运行端结束；再开始一个独立测试任务。
- [ ] 在测试项目触发正常的待审批动作，确认响应回到原请求，原任务继续。
- [ ] 对用户问题、权限与 MCP 请求按实际协议列出支持情况，记录未覆盖项。

LLM 提供最小协议探针或测试程序以及待执行说明；人类完成桌面与手机界面的实际操作。结果至少包含真实关联标识和前后状态，不收集大量截图。

### P0.4 作出实施选择

通过条件：至少一条目标平台的完整控制路径可复现，且已配置的第三方模型可沿用。文档明确区分桌面活动会话和连接器发起会话的支持情况，不将后者通过推导为前者通过。

平台调整规则：

- 路径、后台环境或安装脚本问题先在 Windows 修复。
- 若最小验证证明 Windows 缺失关键桌面控制接口或存在明确版本障碍，记录失败点，再用 macOS 验证同一场景；用户已允许调整平台顺序。
- 若两平台都只能读历史或另起任务，则保留核心场景为未完成。可以继续连接器管理模式的独立工作，但发布范围的缩减必须明确讨论，不能静默降低目标。

产出：固定版本、选定底座、平台顺序、各会话来源的能力表、一个可复现控制样例。将结论同步到 README 和 DESIGN 后进入 P1。

## P1 本机配置与控制

### P1.1 接入本机 Codex

- [ ] 识别并复用选定 Codex 安装，继承实际配置目录、所选配置方案、项目目录和必要环境。
- [ ] 读取脱敏有效配置与模型目录，验证后台启动与手动启动的模型及凭据来源一致。
- [ ] 实现项目与分页会话列表，分别显示无会话、配置不兼容和读取失败。
- [ ] 新建、继续会话时核对实际 provider、model、cwd 与 worktree；不要复制配置到一个空目录后声称沿用成功。
- [ ] 沿用用户的 Codex 访问模式；已配置完全访问时直接使用，中继不增加工具白名单或额外审批。

### P1.2 建立唯一写路径

- [x] 连接器管理的任务接入公开 app-server，保持常驻连接。
- [ ] 对 P0 证明可控的桌面活动会话接入小型桌面控制模块，保持原运行端执行。
- [ ] 建立当前任务与控制路径的关联；打开会话仅观察，写操作在运行端再次校验目标。
- [ ] 完成新一轮、运行中补充、停止、审批和问题回答；明确原生排队与立即补充的不同结果。
- [ ] 任务结束、旧审批过期、目标改变和不支持的接口返回可理解的结果，不自动切到另一运行实例。

验证：必要自动化覆盖新建与恢复配置、发送目标变化、过期审批、运行端错误。用真实 app-server 协议验证主链路，再由人类验证两端控制。

通过条件：在固定支持组合中，配置与真实执行一致；打开不打断；输入、停止与审批影响正确任务。未覆盖的会话来源明确显示能力限制。

## P2 手机 Web 核心体验

### P2.1 最少页面

- [ ] 实现扫码或填写地址与 Token、项目与会话列表、会话详情及轻量设置。
- [ ] 时间线展示回复、计划、命令活动、完成与失败状态；大段输出折叠，历史向上分页。
- [ ] 输入区保留草稿与失败输入，运行中提供补充与停止，待处理请求持续可见。
- [ ] 模型及推理档位来自已配置能力；未知模型保持可用输入，不要求用户换模型。

### P2.2 文件与移动输入

- [ ] 支持 Markdown、文本、代码、常见图片和会话任务差异弹层。
- [ ] 路径与资源读取限制在正确项目边界；差异缺失明确说明，避免展示无关工作区变化。
- [ ] 图片上传按模型能力开放；系统键盘听写直接进入可编辑输入框。
- [ ] 支持触控、软键盘、滚动位置、添加主屏幕的基础体验；退出文件预览返回原会话位置。

验证：对路径穿越、junction/符号链接逃逸、主动内容和超限文件做针对性测试。手机排版、听写、软键盘与弹层由人类操作，不编写截图快照测试。

通过条件：人类能从手机完成选择项目、下达任务、补充要求、审批、查看修改结果的完整流程。此时只用于私有单用户验证。

## P3 发送与恢复

### P3.1 操作结果可靠

- [x] 为输入、停止和请求回应增加稳定操作 ID、载荷一致性校验及连接器持久记录。
- [x] 转发前记录状态，运行端接受后保存关联回执；中继回执不能使 Web 提前显示任务已接受。
- [x] 重连查询原操作结果，已接受结果直接重放；不确定结果保持待核实，禁止盲目重发。
- [x] 明确记录保留与清理规则，过期未知操作要求用户核实，不能作为新操作自动执行。

### P3.2 状态恢复

- [ ] 使用连接代次与事件游标恢复增量，缺失范围回到当前快照和分页历史。
- [ ] 处理快照与增量到达交错，防止重复、遗漏或旧快照覆盖新状态。
- [ ] 恢复仍有效的审批与问题；已经处理或无法从真实请求源恢复的项不再可点击。
- [ ] 手机与中继断线不关闭本机运行连接；重连后同步当前任务和操作状态。
- [ ] 连接器或 Codex 重启时明确暴露实际中断与不确定结果，不自动恢复执行有副作用的任务。

验证：协议故障测试覆盖转发前断开、接受后丢回执、同 ID 重放、载荷变更、快照交错、缓存缺口、双方审批。人类完成锁屏和 Wi-Fi/蜂窝切换。

通过条件：测试矩阵 A05 至 A10 通过，没有静默丢输入或意外重复任务；故障超出保证范围时仍给出真实状态和下一步。

## P4 自托管中继部署

- [x] 提供单节点部署文件、最少环境变量示例和启动说明；首次部署生成实例 Token，用户设置可访问地址。
- [x] 电脑和手机共用实例 Token，统一校验连接及文件入口；连接后开放该实例的项目控制，无账号或逐设备授权。
- [ ] 提供扫码连接和手工地址配置，更换 Token 后断开旧连接；普通重启继续使用原配置。
- [ ] 给出现成反向代理的 HTTPS/WSS 配置，确保手机和电脑主动连出可达；保留重连退避。
- [x] 中继只保存配置与必要日志，在线路由使用内存；核对不落盘聊天正文、文件内容或模型凭据。

验证：正确 Token 可使用、缺失或错误 Token 被拒绝、轮换后旧连接失效；中继重启后客户端恢复。仅做这些基础检查，不增加多租户与角色权限测试。

通过条件：用户按说明部署自己的中继，电脑启动连接器，手机扫码即可控制任务。无需账号数据库、邀请流程或逐设备批准。

## P5 安装分发与维护

- [ ] 生成 Windows 用户可直接启动的安装或便携包，携带连接器依赖，提供托盘或简单本机状态窗口及扫码入口。
- [ ] 使用当前用户身份后台运行，处理登录自启、代理、凭据来源与休眠提示；电脑连接器不要求用户手工维护 Node、Docker 或环境脚本，中继部署按 P4 说明完成。
- [ ] 检测已有 Codex 并给出兼容结果；缺失时提供明确安装引导，不静默替换用户版本或更改模型配置。
- [ ] 首次选择项目限定可访问根目录，日常使用保留选择。卸载只处理连接器自身文件，不删除原生会话和项目。
- [ ] 固定连接器、中继与已验证 Codex 版本组合；不兼容时明确提示，空闲时更新，并保留回退路径。
- [ ] 提供必要错误日志与中继地址、Token 设置；恢复安装后能够重新扫码连接。

若 macOS 已提前成为首验平台，先完成其等价安装路径，随后回补 Windows；两端复用相同业务实现。

验证：人类在未安装连接器开发依赖的环境完成安装、重启后连接、扫码、任务执行和卸载；LLM 负责构建及包内容检查。

通过条件：已配置 Codex 的用户不修改源码或手工启动开发服务即可使用；电脑端模型和原有项目保持正确。

## P6 小范围试用

- [ ] 先完成一台电脑与一部真实手机的完整矩阵，再让少量用户各自部署试用；至少验证一个第三方模型配置。
- [ ] 保存每个验收组合的电脑系统、手机系统/浏览器、Codex/桌面/连接器版本、模型服务与结果，记录失败与恢复方式。
- [ ] 按输入到电脑回执、打开近期会话、前台恢复分别记录耗时；模型完整回复耗时单独观察。
- [ ] 初始体验目标采用正常手机网络下输入到电脑回执 P95 不高于 1 秒、近期会话可操作 P95 不高于 2 秒、小文本预览 P95 不高于 2 秒。这些是待验证目标，不是现有成绩或服务承诺；样本不足时不声称达标。
- [ ] 明确已支持平台、会话来源、模型组合和剩余限制，补齐用户安装说明后再扩大试用。

完成条件：核心场景在公布支持的组合上通过，所有未覆盖项可见，没有另起运行实例冒充当前任务控制。用户选择完全访问模式可正常使用；需要验证原生审批路径时用测试配置触发，不能把没有审批事件记成审批能力已通过。

## 验收矩阵

应用用例仍待人工执行。A07/A08/A10/A12 的部分接口与故障条件已由自动化覆盖，不能替代完整手机验收。

| ID | 场景与操作 | 通过条件 | 方式 |
| --- | --- | --- | --- |
| A01 | 用户原有 DeepSeek 配置启动与续聊 | 实际 provider、模型、目录正确，无额外 OpenAI 登录要求 | 协议检查与人工 |
| A02 | 手机上打开电脑活动会话 | 实时进度可见，原任务继续，无第二执行实例 | 协议检查与人工 |
| A03 | 运行中发送补充，随后点击停止 | 补充进入目标任务或明确原生排队；停止得到原任务确认 | 自动化与人工 |
| A04 | 发出命令审批与普通用户问题 | 手机上正确回应，原任务继续，两端状态一致 | 协议检查与人工 |
| A05 | 锁屏 5 分钟后返回 | 任务未因手机锁屏被终止，补齐输出及有效待处理项 | 人工 |
| A06 | Wi-Fi 与蜂窝切换 | 自动重连、草稿保留，已接受消息不重复执行 | 人工 |
| A07 | 运行端已接受但回执丢失 | 显示已核实结果或待核实，不自动创建重复任务 | 故障测试 |
| A08 | 手机和电脑同时处理同一审批 | 只产生一次有效决策，另一端显示已处理 | 自动化与人工 |
| A09 | 中继重启 | 电脑运行连接保持，手机恢复正确状态 | 故障测试与人工 |
| A10 | 连接器或 Codex 重启 | 恢复历史与操作记录，准确说明任务是否中断，无隐式重跑 | 故障测试 |
| A11 | 自己的多个会话或机器使用相似路径和会话名 | 输入、事件、审批、下载始终对应选中的机器与会话 | 自动化与人工 |
| A12 | 项目含 worktree、链接跳转与超限文件 | 文件来自正确目录，越界被拒绝，大文件合理降级 | 自动化与人工 |
| A13 | 长会话、失败加载与快速切换会话 | 分页可用、原阅读位置合理保留，不显示上一会话结果 | 人工 |
| A14 | 缺失或错误 Token，以及更换 Token 后重连 | 无有效 Token 不能访问；轮换断开旧连接，新配置可正常连接 | 自动化与人工 |
| A15 | 升级 Codex 或连接器 | 核心输入、审批、停止回归通过，或明确阻止不兼容操作 | 协议检查与人工 |
| A16 | 不具备图片能力的模型与未知模型目录 | 不误导用户发送不支持内容，不静默换模型 | 自动化与人工 |
| A17 | 自托管部署、电脑安装与后台启动 | 按说明部署中继；连接器无开发依赖手工配置，后台模型环境正确 | 人工 |

权限请求与 MCP 问询若存在于支持组合，追加 A04 的子用例并保留真实类型和结果，不能只用普通聊天问题覆盖。

## 实施时的代码落点

以下是职责分区，不要求为了匹配名称重排候选仓库：

| 位置 | 内容 |
| --- | --- |
| `web/` | 连接配置、项目与会话、输入、请求卡片、文件弹层及草稿 |
| `src/connector/` | 本机配置、公开 app-server、桌面控制模块、操作记录与恢复 |
| `src/server/` | Token 校验、内存路由、配置与中继生命周期 |
| `src/shared/` | 实际跨端使用的消息类型和状态；不放通用框架 |
| `test/` | 控制目标、配置继承、故障恢复、基础 Token 校验和文件边界测试 |
| `scripts/` 与 `deploy/` | 本地开发、构建打包和中继部署 |

复用底座时先跑其现有类型检查、必要测试和构建，以实际 package scripts 为准。目录与构建、测试、启动命令已实现；部署文件位于项目根目录，Windows 启动脚本在 scripts/windows/。

## 实施记录

| 项目 | 2026-09-06 记录 |
| --- | --- |
| 平台 | Windows 11 Home 10.0.26200，x64；原生 Windows，未使用 WSL |
| CLI | npm Codex 0.146.0；解析用户现有安装，不替换版本；探测时也发现桌面托管 0.153.4，但未作为默认连接器运行时 |
| 桌面 | 26.901.6511.0；真实 IPC 握手、当前活动会话快照、canonical 历史读取通过 |
| 用户配置 | 当前用户 ~/.codex；有效模型 gpt-6-astra，provider openai；未发现现有 DeepSeek 配置，未读取或输出 auth.json 正文 |
| 唯一实现方向 | 小型独立 TypeScript 项目，候选提交、许可证与弃用原因见 SOURCES.md |
| 自动化 | `npm run check`：类型检查与 14 项测试通过；覆盖桌面帧与增量缺口、当前任务目标、原生请求类型、去重与重连、Token、文件越界 |
| 真实 app-server | `npm run test:native` 通过；隔离临时 CODEX_HOME + 本地 Responses 夹具，验证模型/provider/环境 Key、原访问模式、首条消息前空会话、发送、流式读取、steer 和 interrupt |
| 真实第三方服务 | 未连接 DeepSeek；夹具不使用外部模型账户，不作为服务兼容成绩 |
| 构建与部署 | 前端 dist + Node build 构建成功；本机中继与连接器后台运行；Windows 便携目录含运行时与许可证；Docker 部署文件未实测 |
| 完整读取链路 | `scripts/status.ts` 经实际中继和连接器读到本条活动桌面会话，source=desktop、active=true，内容与操作目标可识别 |
| 原生桌面写路径 | 已按本机接口与模拟测试实现；没有对重要当前任务发送测试消息或停止，真实发送/停止/审批保持待人工验收 |
| 下一任务 | 按 QUICKSTART 的 6 步做手机与桌面验证，优先 A02–A04，然后 DeepSeek 和换网；根据真实失败修复 |

CLI 命名 profile 经原生命令核对不支持 app-server，已改为启动时明确报错，默认模型配置继承通过；桌面既有会话仍使用其自身配置。

本次额外修复了三个兼容点：Windows npm 可执行文件的新 bin 布局、桌面新的 canonical 历史结构、原生空会话在首条用户消息前不能 includeTurns 的限制。恢复采用变更通知加当前快照，省去额外事件回放系统。操作记录保留 7 天、未知新到操作仅接受 5 分钟窗口，断线结果不明时不自动重发。

待完成：手机与真实桌面操作验收、新电脑安装及自启实测、公网 Docker/HTTPS 验证、真实 DeepSeek、macOS。大文件下载、超过最近 100 轮的历史、嵌套 MCP 表单、托盘和自动更新未实现，按需补齐；不把这些阶段标成已验收。

## 2026-09-06 UX 调整

依据用户提供的三张截图，已替换为白色移动布局、项目/最近会话首页、底部搜索、胶囊导航、新聊天居中项目选择和悬浮输入。模型、图片、文件收进底部面板，保留停止与必要反馈。语音入口沿用键盘听写。

新聊天延迟至首次发送时创建，新增 3 个必要故障测试：创建确认后才发送、创建回执不明不继续、首条消息回执丢失不重发。搜索使用原生 thread/list 的 searchTerm。类型检查和共 14 项测试通过，界面构建通过；视觉、软键盘、返回及触控验收仍交由人类完成。
