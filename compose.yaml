services:
  relay:
    build: .
    restart: unless-stopped
    environment:
      BRIDGE_TOKEN: ${BRIDGE_TOKEN:?Set BRIDGE_TOKEN in .env}
    ports:
      - "127.0.0.1:3340:3340"
    read_only: true
    tmpfs:
      - /tmp
